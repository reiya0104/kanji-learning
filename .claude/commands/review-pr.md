以下の手順でセルフレビューを実施し、PR を作成してください。

## ステップ1：変更内容の把握

対応する Issue 番号を確認してください（不明な場合はユーザーに確認）。

```bash
git log main..HEAD --oneline
git diff main..HEAD --stat
```

## ステップ2：スコープ外変更チェック

Issue のタイトル・やること と `git diff main..HEAD` を照合し、以下を確認してください：

- [ ] Issue のスコープ外のファイルが変更されていないか
- [ ] 意図しない変更（デバッグコード、無関係なリファクタ等）が混入していないか

スコープ外の変更が見つかった場合はユーザーに指摘し、別 Issue・別 PR に分けることを提案してください。

## ステップ3：PR description のドラフト

PR テンプレートに沿って description を生成してください：

```
## 変更内容

（変更の概要を箇条書きで）

## 対応 Issue

Closes #XX

## 影響範囲

（変更によって影響を受ける機能・ファイル）

## 確認項目
- [ ] lint 通過
- [ ] typecheck 通過
- [ ] test 通過
```

## ステップ4：ユーザーに確認

ステップ2のチェック結果とステップ3のドラフトをまとめてユーザーに提示し、承認を得てください。

## ステップ5：PR 作成

承認後、以下のコマンドで PR を作成してください：

```bash
gh pr create \
  --title "<prefix>: <変更内容>" \
  --body "$(cat <<'EOF'
（ステップ3のドラフト）
EOF
)"
```

PR 作成後、URL をユーザーに提示してください。
